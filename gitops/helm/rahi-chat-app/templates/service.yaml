apiVersion: v1
kind: Service
metadata:
  name: {{ include "rahi-chat-app.fullname" . }}
  labels:
    {{- include "rahi-chat-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rahi-chat-app.selectorLabels" . | nindent 4 }}
