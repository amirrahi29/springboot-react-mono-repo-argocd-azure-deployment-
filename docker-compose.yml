# Production-like single image (backend serves API + built SPA).
services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8081:8081"
    environment:
      SERVER_PORT: "8081"
